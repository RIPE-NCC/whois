#
# Application version. Don't store anything else in here!
#

# public release version (tagged in repo)
application.version=1.120-SNAPSHOT

# commit-id and timestamp (generated by the build)
application.build.timestamp=${maven.build.timestamp}
application.build.commit.id=${git.commit.id.abbrev}
