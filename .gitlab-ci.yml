image: $CI_REGISTRY/swe-database-team/gitlab-ci/whois-build:chrome80-jdk8-maven36-node10

variables:
  MAVEN_OPTS: "-Dmaven.repo.local=.m2/repository"
  TZ: Europe/Amsterdam
  WHITESOURCE_PRODUCT: "database-team"
  WHITESOURCE_PROJECT: "whois"

include:
  - project: 'swe/gitlab-ci'
    file: '/templates/whitesource-scanning.yml'

stages:
  - build
  - test
  - code_quality
  - deploy

cache:
  key: ${CI_JOB_NAME}
  paths:
    - .m2/repository
    - target/
    - "*/target/*"

build:
  stage: build
  script:
    - mvn $MAVEN_OPTS clean install -U -Prelease
  artifacts:
    paths:
      - whois-db/target/whois-db-*-SNAPSHOT.jar
    reports:
      junit:
        - "*/target/surefire-reports/TEST-*.xml"
        - "*/target/failsafe-reports/TEST-*.xml"

.it_template: &it_env
  services:
    - mariadb:10.2
  variables:
    MYSQL_ROOT_PASSWORD: root
    TZ: Europe/Amsterdam
  stage: test
  before_script:
    - mysql -uroot -proot -h mariadb -e "CREATE USER 'dbint'@'%' IDENTIFIED BY '';"
    - mysql -uroot -proot -h mariadb -e "GRANT ALL ON *.* TO 'dbint'@'%' WITH GRANT OPTION;"
    - mysql -uroot -proot -h mariadb -e "SET GLOBAL innodb_file_per_table = 0;"
    - mysql -uroot -proot -h mariadb -e "SET GLOBAL innodb_buffer_pool_size = 2147483648;"
  artifacts:
    reports:
      junit:
        - "*/target/surefire-reports/TEST-*.xml"
        - "*/target/failsafe-reports/TEST-*.xml"
  when: manual
  allow_failure: false

whois-it-suite1:
  <<: *it_env
  tags:
    - whois
  script:
    - mvn -DskipTests install
    - cd whois-endtoend && mvn $MAVEN_OPTS -Ddb.host=mariadb verify -Pall
    - find . -name *.exec
    #- cp whois-endtoend/target/jacoco.exec whois-endtoend/target/jacoco_whois-endtoend.exec
    #- cp whois-endtoend/target/jacoco-it.exec whois-endtoend/target/jacoco-it_whois-endtoend.exec

whois-it-suite2:
  <<: *it_env
  tags:
    - whois
  script:
    - mvn -DskipTests install
    - cd whois-api && mvn $MAVEN_OPTS -Ddb.host=mariadb verify -Pintegration
    - find . -name *.exec
    #- cp whois-api/target/jacoco.exec whois-api/target/jacoco_whois-api.exec
    #- cp whois-api/target/jacoco-it.exec whois-api/target/jacoco-it_whois-api.exec
    - cd whois-client && mvn $MAVEN_OPTS -Ddb.host=mariadb verify -Pintegration
    #- find . -name *.exec
    #- cp whois-client/target/jacoco.exec whois-client/target/jacoco_whois-client.exec
    #- cp whois-client/target/jacoco-it.exec whois-client/target/jacoco-it_whois-client.exec
    - cd whois-commons && mvn $MAVEN_OPTS -Ddb.host=mariadb verify -Pintegration
    - find . -name *.exec
    #- cp whois-commons/target/jacoco.exec whois-commons/target/jacoco_whois-commons.exec
    #- cp whois-commons/target/jacoco-it.exec whois-commons/target/jacoco-it_whois-commons.exec
    - cd whois-db && mvn $MAVEN_OPTS -Ddb.host=mariadb verify -Pintegration
    - find . -name *.exec
    #- cp whois-db/target/jacoco.exec whois-db/target/jacoco_whois-db.exec
    #- cp whois-db/target/jacoco-it.exec whois-db/target/jacoco-it_whois-db.exec
    - cd whois-nrtm && mvn $MAVEN_OPTS -Ddb.host=mariadb verify -Pintegration
    - find . -name *.exec
    #- cp whois-nrtm/target/jacoco.exec whois-nrtm/target/jacoco_whois-nrtm.exec
    #- cp whois-nrtm/target/jacoco-it.exec whois-nrtm/target/jacoco-it_whois-nrtm.exec
    - cd whois-query && mvn $MAVEN_OPTS -Ddb.host=mariadb verify -Pintegration
    - find . -name *.exec
    #- cp whois-query/target/jacoco.exec whois-query/target/jacoco_whois-query.exec
    #- cp whois-query/target/jacoco-it.exec whois-query/target/jacoco-it_whois-query.exec
    - cd whois-rpsl && mvn $MAVEN_OPTS -Ddb.host=mariadb verify -Pintegration
    - find . -name *.exec
    #- cp whois-rpsl/target/jacoco.exec whois-rpsl/target/jacoco_whois-rpsl.exec
    #- cp whois-rpsl/target/jacoco-it.exec whois-rpsl/target/jacoco-it_whois-rpsl.exec
    - cd whois-scheduler && mvn $MAVEN_OPTS -Ddb.host=mariadb verify -Pintegration
    - find . -name *.exec
    #- cp whois-scheduler/target/jacoco.exec whois-scheduler/target/jacoco_whois-scheduler.exec
    #- cp whois-scheduler/target/jacoco-it.exec whois-scheduler/target/jacoco-it_whois-scheduler.exec
    - cd whois-update && mvn $MAVEN_OPTS -Ddb.host=mariadb verify -Pintegration
    - find . -name *.exec
    #- cp whois-update/target/jacoco.exec whois-update/target/jacoco_whois-update.exec
    #- cp whois-update/target/jacoco-it.exec whois-update/target/jacoco-it_whois-update.exec

sonar:
  stage: code_quality
  script:
    - find . -name *.exec
    - cp whois-endtoend/target/jacoco_whois-endtoend.exec whois-scheduler/target/jacoco.exec
    - cp whois-endtoend/target/jacoco-it_whois-endtoend.exec whois-scheduler/target/jacoco-it.exec
    - cp whois-api/target/jacoco_whois-api.exec whois-api/target/jacoco.exec
    - cp whois-api/target/jacoco-it_whois-api.exec whois-api/target/jacoco-it.exec
    - cp whois-client/target/jacoco_whois-client.exec whois-client/target/jacoco.exec
    - cp whois-client/target/jacoco-it_whois-client.exec whois-client/target/jacoco-it.exec
    - cp whois-commons/target/jacoco_whois-commons.exec whois-commons/target/jacoco.exec
    - cp whois-commons/target/jacoco-it_whois-commons.exec whois-commons/target/jacoco-it.exec
    - cp whois-db/target/jacoco_whois-db.exec whois-db/target/jacoco.exec
    - cp whois-db/target/jacoco-it_whois-db.exec whois-db/target/jacoco-it.exec
    - cp whois-nrtm/target/jacoco_whois-nrtm.exec whois-nrtm/target/jacoco.exec
    - cp whois-nrtm/target/jacoco-it_whois-nrtm.exec whois-nrtm/target/jacoco-it.exec
    - cp whois-query/target/jacoco_whois-query.exec whois-query/target/jacoco.exec
    - cp whois-query/target/jacoco-it_whois-query.exec whois-query/target/jacoco-it.exec
    - cp whois-rpsl/target/jacoco_whois-rpsl.exec whois-rpsl/target/jacoco.exec
    - cp whois-rpsl/target/jacoco-it_whois-rpsl.exec whois-rpsl/target/jacoco-it.exec
    - cp whois-scheduler/target/jacoco_whois-scheduler.exec whois-scheduler/target/jacoco.exec
    - cp whois-scheduler/target/jacoco-it_whois-scheduler.exec whois-scheduler/target/jacoco-it.exec
    - cp whois-update/target/jacoco_whois-update.exec whois-scheduler/target/jacoco.exec
    - cp whois-update/target/jacoco-it_whois-update.exec whois-scheduler/target/jacoco-it.exec
    - mvn sonar:sonar -Dsonar.host.url=http://sonar.ripe.net -Dsonar.projectVersion=$CI_COMMIT_SHORT_SHA -Dsonar.projectKey=whois -Dsonar.projectName=whois

.deploy_template: &deploy_env
  stage: deploy
  before_script:
    - mkdir -p /root/.ssh
    - touch /root/.ssh/known_hosts
    - ssh-keyscan hanwoo.ripe.net 2>&1 >>/root/.ssh/known_hosts
    - echo "search ripe.net">>/etc/resolv.conf
    - eval $(ssh-agent -s)
    - echo -e "Host *\n\tStrictHostKeyChecking no\n\nAddressFamily inet\n\n" > ~/.ssh/config
    - echo "$DBASE_SSH_KEY" | tr -d '\r' | ssh-add -
  script:
    - git clone http://dbase:$DB_OPERATIONAL_DEPLOY_TOKEN@gitlab.ripe.net/swe-database-team/db-operational.git
    - find whois-db/target -name whois-db-*-SNAPSHOT.jar -exec cp {} db-operational/deployment/whois-db-$CI_COMMIT_SHA.jar \;
    - cd db-operational/deployment
    - ./stage.sh -p whois -e $CI_ENVIRONMENT_NAME whois-db-$CI_COMMIT_SHA.jar
    - ./sync.sh -p whois -e $CI_ENVIRONMENT_NAME
    - bash ./deploy.sh -p whois -e $CI_ENVIRONMENT_NAME

deploy_dev:
  <<: *deploy_env
  environment:
    name: dev
  when: manual

deploy_prepdev:
  <<: *deploy_env
  environment:
    name: prepdev
  when: manual

deploy_rc:
  <<: *deploy_env
  environment:
    name: rc
  when: manual

deploy_training:
  <<: *deploy_env
  environment:
    name: training
  when: manual

deploy_test:
  <<: *deploy_env
  environment:
    name: test
  when: manual

deploy_prod:
  <<: *deploy_env
  environment:
    name: prod
  when: manual
